create or replace function checkingIfOrdered(orderId in number, memberPhone in number, currentMealName in varchar2) return boolean is
  FunctionResult boolean;
begin
  
  return(FunctionResult);
end checkingIfOrdered;
/
